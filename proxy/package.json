{
  "name": "njia-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Njia Claude proxy — holds the API key, injects the verified knowledge pack, answers /ask",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
